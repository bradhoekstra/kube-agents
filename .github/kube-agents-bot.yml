# Live-test artifact for gke-labs/kube-agents-bot#feat/in-repo-config.
# Safe to delete. Set to on: the staging bot writes the assignee field, while
# the deployment's own ASSIGNEE_MANAGEMENT stays off.
assignees: on
