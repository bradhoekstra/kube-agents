# Staging check: the review's artifacts reach the bucket

Proving-ground pull request for gke-labs/kube-agents-bot#76 (second half). It exists so the
staging bot has a change to review whose candidates, verification batches and manifest can then be
read back out of the artifacts bucket. Do not merge.
