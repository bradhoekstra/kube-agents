# Assignee rule smoke test

Throwaway. Opened to exercise `app/ownership.py` against the staging install
(gke-labs/kube-agents-bot#24). Delete the branch and close the pull request when
the walkthrough is done.
